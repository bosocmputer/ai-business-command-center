{
  "name": "ai-business-command-center",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@10.10.0",
  "scripts": {
    "dev": "(cd packages/shared && corepack pnpm build) && (cd packages/reports && corepack pnpm build) && corepack pnpm --parallel --filter @ai-bcc/api --filter @ai-bcc/web dev",
    "dev:web": "corepack pnpm --filter @ai-bcc/web dev",
    "dev:api": "corepack pnpm --filter @ai-bcc/api dev",
    "build": "corepack pnpm -r build",
    "typecheck": "(cd packages/shared && corepack pnpm build) && (cd packages/reports && corepack pnpm build) && corepack pnpm -r typecheck",
    "test": "corepack pnpm -r test",
    "lint": "corepack pnpm --filter @ai-bcc/web lint"
  }
}
